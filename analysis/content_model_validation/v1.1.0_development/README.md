# CMACM v1.1.0 Atom-First Development Retest

This run tests the same 15 development reports that exposed the v1.0.3 flat
entity-count failure. It uses complete annotated Docling Markdown, where every
source block remains present and receives a deterministic document atom ID.
Terra returns typed atomic attributes and evidence atom IDs; Python derives
WHO Foundation entities using frozen identity keys.

All 45 runs passed schema, reference and evidence-atom validation. Derived
workflow and link stability improved but remained below production use. See
`diagnostic.md` and `derived_structure_stability.json`. The next sample must be
new and fully disjoint after a method addressing atomic recall and attribute
classification is developed.

No eligibility, PRISMA or synthesis count changed.
