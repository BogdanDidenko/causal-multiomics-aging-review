{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "completed_current_report",
    "applied_design_signal",
    "directional_result_signal",
    "exposure_or_intervention",
    "comparator",
    "outcome",
    "estimand_or_contrast",
    "evidence_spans",
    "boundary_case",
    "uncertainty_reason",
    "concise_rationale"
  ],
  "properties": {
    "completed_current_report": {"enum": ["yes", "no", "unclear"]},
    "applied_design_signal": {"enum": ["yes", "no", "unclear"]},
    "directional_result_signal": {"enum": ["yes", "no", "unclear"]},
    "exposure_or_intervention": {"type": "string"},
    "comparator": {"type": "string"},
    "outcome": {"type": "string"},
    "estimand_or_contrast": {"type": "string"},
    "evidence_spans": {
      "type": "array",
      "items": {"$ref": "#/$defs/title_abstract_evidence"}
    },
    "boundary_case": {
      "enum": [
        "clear_identified",
        "thin_abstract",
        "causal_wording_only",
        "prediction_or_noncausal",
        "colocalization_only",
        "network_or_direction_only",
        "rct_without_mediation_identification",
        "uncontrolled_mediation",
        "validation_design_secondary",
        "other",
        "none"
      ]
    },
    "uncertainty_reason": {"type": "string"},
    "concise_rationale": {"type": "string", "minLength": 1}
  },
  "$defs": {
    "title_abstract_evidence": {
      "type": "object",
      "additionalProperties": false,
      "required": ["criterion", "source", "quote"],
      "properties": {
        "criterion": {
          "enum": [
            "completed_current_report",
            "applied_design_signal",
            "directional_result_signal",
            "exposure_or_intervention",
            "outcome",
            "estimand_or_contrast"
          ]
        },
        "source": {"enum": ["title", "abstract", "metadata"]},
        "quote": {"type": "string", "minLength": 1}
      }
    }
  }
}
