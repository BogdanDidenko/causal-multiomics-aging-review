# LEGACY: NOT FOR PRISMA

This directory contains superseded graph-prioritized full-text screening
inputs and results. It is retained only for audit and methodological
postmortem. Use suite `v1.5.3-rc1` and the deterministic merged corpus instead.
